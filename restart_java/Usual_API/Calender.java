package Usual_API;

public class Calender {
    /*
        代表的是系统此刻时间对应的日历，通过它可以单独获取、修改时间中的年、月、日、时、分、秒等

        Calender getInstance()  获取当前日历对象
        int get(int field)  获取日历中的某个信息
        Date getTime()  获取日期对象
        long getTimeInMillis() 获取时间毫秒值
        void set(int field,int value)   修改日历的某个信息
        void add(int field,int amount)  为某个信息增加/减少指定值
     */
}
