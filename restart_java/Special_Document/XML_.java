package Special_Document;

public class XML_ {
    /*
        XML(全称EXtensible Markup Language,可扩展标记语言)
            本质是一种数据的格式，可以用来存储存储复杂的数据结构，和数据关系

        XML的特点
            XML的"<标签名>"称为一个标签或一个元素，一般是成对出现的
            XML中的标签名可以自己定义（可扩展），但必须要正确的嵌套
            XML中只能有一个根标签
            XML中的标签可以又属性
            如果一个文件中放置的是XML格式的数据，中国文件就是XML文件，后缀一般要写成.xml

        XML的语法规则
            XML中书写< &等，可能会出现冲突，导致报错，此时可以用如下特殊字符替代
            &lt;    <   小于
            &gt;    >   大于
            &amp;   &   和号
            &apos;  '   单引号
            &quot;  "   引号

        XML中可以写一个CDATA的数据区:<![CDATA[ ...内容... ]]>,里面的内容可以随便写
     */
}
