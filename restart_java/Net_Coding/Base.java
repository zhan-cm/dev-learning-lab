package Net_Coding;

public class Base {
    /*
        InetAddress
        public static InetAddress getLocalHost()    获取本机IP，会以一个InetAddress对象返回
        public static InetAddress getByName(String host)    根据IP地址或域名，返回一个InetAddress对象
        public String getHostName() 获取该IP地址对象对应的主机名
        public String getHostAddress()  获取该IP地址对象中的IP地址信息
        public boolean isReachable(int timeout) 在指定毫秒内，判断主机与该IP对应的主机是否能连通

        port
        端口：标记正在计算机设备上运行的应用程序的，被规定一个16位二进制，范围是0-65535
        分类：
            周知端口：0-1023，被预先定义的知名应用程序占用
            注册端口：1024-49151，分配给用户进程或某些应用程序
            动态端口：49152到65535，之所以成为动态端口，是因为它一般不固定分配某种进程，而是动态分配
     */
}
