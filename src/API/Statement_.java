package API;

public class Statement_ {
    /*
        Statement作用：
            1.执行SQL语句
                int executeUpdate(sql):执行DML、DDL语句
                返回值：(1)DML语句影响的行数   (2)DDL语句执行后，执行成功也可能返回0

                ResultSet execute(sql):执行DQL语句
                返回值：ResultSet结果集对象
     */
}
